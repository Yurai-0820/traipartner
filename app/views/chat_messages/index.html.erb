<div class="row">
  <div class="col-8 offset-2">
    <div class="text-center">
     
      <p><%= @tag.title %></p>
    
      <p><%= @tag.content %></p>
      
      <%= render 'tag_relations/tag_add_button', tag: @tag if user_signed_in? %>
      
      <% if user_signed_in? && current_user.id == @tag.user.id %>
        <%= link_to 'チャットルーム削除', tag_path(tag), method: :delete, class: 'btn btn-danger tag-delete-button' %>
      <% end %>
      
      <div>
        <p class='text-center'>参加しているユーザー</p>
        <%= render 'users/users', users: @users %>
      </div>

<div class="messages">

  <%# <%= render 'message', collection: @chat_messages %> 
</div>


<%= form_with model: [@tag_id, @chat_message], url: tag_chat_messages_path ,id: 'new_chat',class: 'form', local: true do |f| %>
  <div class="form-input">
    <%= f.text_field :content, class: 'form-message', placeholder: 'type a message' %>
    <label class="form-image">
      <span class="image-file">画像</span>
      <%= f.file_field :image, class: 'hidden' %>
    </label>
  </div>
  <%= f.submit '送信', class: 'form-submit' %>
<% end %>
    </div>
  </div>
</div>

